<UserControl x:Class="ExpenseTracker.View.ExpenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid>
            <TextBlock Text="💸 Expenses" 
                       FontSize="20" 
                       HorizontalAlignment="Left" 
                       FontWeight="Bold" 
                       Margin="0,0,0,10"/>
            <Button x:Name="btnAddExpense" 
                    Content="Add Expense" 
                    HorizontalAlignment="Right" 
                    Height="30" 
                    Width="140" 
                    Click="btnAddExpense_Click"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" IsReadOnly="True"
                  ItemsSource="{Binding Expenses}"
                  SelectedItem="{Binding SelectedExpense}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,10,0,0">

            <DataGrid.Columns>
                <DataGridTextColumn MinWidth="120" Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn MinWidth="100" Header="Amount" Binding="{Binding Amount}" />
                <DataGridTextColumn MinWidth="120" Header="Date" Binding="{Binding DateOfExpense}" />
                <DataGridTextColumn MinWidth="150" Header="Description" Binding="{Binding Description}" />
                <DataGridTextColumn MinWidth="120" Header="Category" Binding="{Binding Category}" />
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edit" Click="EditExpense_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>
