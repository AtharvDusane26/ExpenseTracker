<UserControl x:Class="ExpenseTracker.View.UserMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <!-- Expenses -->
            <TextBlock Text="💵 Expenses" FontSize="18" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding ExpenseInsights}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="5,2" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Savings -->
            <TextBlock Text="💰 Savings" FontSize="18" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding SavingsInsights}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="5,2" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Goals -->
            <TextBlock Text="🎯 Goals" FontSize="18" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding GoalInsights}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="5,2" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Outcomes -->
            <TextBlock Text="📅 Upcoming Outcomes" FontSize="18" FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding OutcomeInsights}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="5,2" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Prediction -->
            <TextBlock Text="📈 Prediction" FontSize="18" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding PredictedExpenses}" Margin="5,2"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
