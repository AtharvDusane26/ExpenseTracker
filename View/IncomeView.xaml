<UserControl x:Class="ExpenseTracker.View.IncomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <TextBlock Text="💰 Income" FontSize="20" HorizontalAlignment="Left" FontWeight="Bold" Margin="0,0,0,10"/>
            <Button x:Name="btnAddIncome" Content="Add Income" HorizontalAlignment="Right" Height="30" Width="140" Click="btnAddIncome_Click"/>
        </Grid>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Incomes}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                <DataGridTextColumn Header="Source" Binding="{Binding SourceOfIncome}" />
                <DataGridTextColumn Header="Day" Binding="{Binding DayOfTransaction}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
