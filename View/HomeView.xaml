<UserControl x:Class="ExpenseTracker.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="🏠 Home Dashboard" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,5">
                <StackPanel>
                    <TextBlock Text="{Binding UserBalance, StringFormat=Balance: {0:C}}" FontSize="16"/>
                    <TextBlock Text="{Binding SavingsBalance, StringFormat=Savings: {0:C}}" FontSize="16"/>
                </StackPanel>
            </Border>

            <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,5">
                <StackPanel>
                    <TextBlock Text="🔔 Notifications" FontWeight="Bold" FontSize="16"/>
                    <ItemsControl ItemsSource="{Binding Notifications}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
