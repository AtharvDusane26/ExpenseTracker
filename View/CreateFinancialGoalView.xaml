<UserControl x:Class="ExpenseTracker.View.CreateFinancialGoalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             Height="400" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">

            <!-- Goal Info -->
            <GroupBox Header="Goal Details" Padding="10">
                <StackPanel Orientation="Vertical" >
                    <TextBlock Text="Goal Name:" FontWeight="Bold"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Target Amount:" FontWeight="Bold"/>
                    <TextBox Text="{Binding TargetAmount, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Duration (Years):" FontWeight="Bold"/>
                    <TextBox Text="{Binding DurationInYears, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Monthly Interest Rate (%):" FontWeight="Bold"/>
                    <TextBox Text="{Binding MonthlyInterestRate, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Check Monthly Installment" Margin="3" Width="250" Click="Check_Contribution"/>
            <Button Content="Save" Width="100" Margin="3" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
