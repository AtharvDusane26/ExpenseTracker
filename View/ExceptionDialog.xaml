<Window x:Class="ExpenseTracker.View.ExceptionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExpenseTracker.View"
        mc:Ignorable="d"
        Height="250" Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <Style TargetType="ScrollViewer" BasedOn="{x:Null}"/>
        <Style TargetType="ScrollBar" BasedOn="{x:Null}"/>
        <Style TargetType="ContextMenu" BasedOn="{x:Null}"/>
        <Style TargetType="MenuItem" BasedOn="{x:Null}"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="DescriptionBox"
                 Grid.Row="0"
                 Margin="2"
                 TextWrapping="Wrap"
                 FontWeight="Bold"
                 FontSize="14"
                 IsReadOnly="True"
                 BorderThickness="0"
                 Background="Transparent"
                 />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button x:Name="QuitButton" Content="Quit" Width="100" Margin="0,0,10,0" Click="QuitButton_Click"/>
            <Button x:Name="DetailButton" Content="Details >>" Width="100" Margin="0,0,10,0" Click="DetailButton_Click"/>
        </StackPanel>
        <TreeView x:Name="DetailTree"
                  Grid.Row="2"
                  Visibility="Collapsed"
                  ContextMenuOpening="DetailTree_ContextMenuOpening">
        </TreeView>
        
    </Grid>

</Window>
