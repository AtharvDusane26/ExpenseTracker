<UserControl x:Class="ExpenseTracker.View.CreateExpenseEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             Height="400" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <!-- Name -->
            <TextBlock Text="Name:" FontWeight="Bold"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Amount -->
            <TextBlock Text="Amount:" FontWeight="Bold"/>
            <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Date -->
            <TextBlock Text="Date:" FontWeight="Bold"/>
            <DatePicker SelectedDate="{Binding DateOfExpense}" />

            <!-- Description -->
            <TextBlock Text="Description:" FontWeight="Bold"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True"  
                     Height="60"/>

            <!-- Category -->
            <TextBlock Text="Category:" FontWeight="Bold"/>
            <TextBox Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Freeze -->
            <CheckBox Content="Freeze Transaction" IsChecked="{Binding Freeze}" />
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Save" Width="100" Margin="0,0,10,0" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
