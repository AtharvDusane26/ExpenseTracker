<UserControl x:Class="ExpenseTracker.View.FinancialGoalsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="🎯 Financial Goals" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <DataGrid ItemsSource="{Binding Goals}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Target" Binding="{Binding TargetAmount}" />
                    <DataGridTextColumn Header="Duration (Months)" Binding="{Binding DurationInMonths}" />
                    <DataGridTextColumn Header="Monthly Contribution" Binding="{Binding MonthlyContribution}" />
                    <DataGridTextColumn Header="Required Contribution" Binding="{Binding RequiredMonthlyContribution}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
