<UserControl x:Class="ExpenseTracker.View.WithdrawSavingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
            Height="150" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Balance:" VerticalAlignment="Center" Margin="10"/>
            <TextBlock Text="{Binding Amount, StringFormat=Rs.{0:N2}}" VerticalAlignment="Center" Margin="10"/>
            <TextBlock Text="Saving Balance:" VerticalAlignment="Center" Margin="10"/>
            <TextBlock Text="{Binding SavingAmount, StringFormat=Rs.{0:N2}}" VerticalAlignment="Center" Margin="10"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">
            <TextBlock Text="Enter Amount to Withdraw:" VerticalAlignment="Center" Margin="10"/>
            <TextBox Width="150" Text="{Binding AmountToWithdraw, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="10"/>
            <Button Grid.Row="1" Content="Withdraw" Width="100" Height="30" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
