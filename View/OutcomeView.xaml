<UserControl x:Class="ExpenseTracker.View.OutcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="💸 Outcome" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

            <DataGrid ItemsSource="{Binding Outcomes}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" />
                    <DataGridTextColumn Header="Type" Binding="{Binding OutComeType}" />
                    <DataGridTextColumn Header="Day" Binding="{Binding DayOfTransaction}" />
                    <DataGridCheckBoxColumn Header="Freeze" Binding="{Binding Freeze}" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
